<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sau22rane.sqlfunctionality">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Edit_Budget_Popup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".Edit_Expenditure_Popup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".OptionExpenditurePopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".OptionsBudgetPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".AddExpenditurePopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".Expenditure_activity" />
        <activity android:name=".AddBudgetPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".Budget_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>